#include<bits/stdc++.h>
using namespace std;

int fact(int m)
{
    if(m>0)
    {
        return m*fact(m-1);
    }
    else
        return 1;
}

int main()
{
    int n;
    cin>>n;

    cout<<fact(n);

    return 0 ;
}
